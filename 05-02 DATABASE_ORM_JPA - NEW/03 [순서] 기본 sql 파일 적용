01 application.properties 설정

# sql Script 로 초기화 설정 모드    (always : 항상 , embedded : 내장 DB만 , never : 초기화수행 안함)
spring.sql.init.mode=always
# script 파일이 hibernate 초기화 이후 동작하게 하기 위한 옵션(@Entity 로 테이블 생성 이후에 동작)
spring.jpa.defer-datasource-initialization=true

02 src/main/resources/schema.sql 설정
Schema.sql 설정

02 src/main/resources/data.sql 설정



